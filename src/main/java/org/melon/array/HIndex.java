package org.melon.array;

import java.util.Arrays;

/**
 * <a href="https://leetcode-cn.com/problems/h-index/">274. H 指数</a>
 */
public class HIndex {

    public int hIndex(int[] citations) {
        int h = 0;
        Arrays.sort(citations);
        for (int i = 0; i < citations.length; i++) {


        }

        return h;
    }
}
